pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "Plantapp"
include(":app")
include(":capacitor-android")
include(":capacitor-cordova-android-plugins")
